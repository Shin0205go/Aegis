# 実践的な使用例

AEGISの実際の使用シナリオと、それぞれのケースでのポリシー設定例を紹介します。

## 📚 基本的な使用例

### 1. 読み取り専用の開発環境

**シナリオ**: ジュニア開発者に、コードの閲覧は許可するが変更は禁止したい

**ポリシー設定**:
```javascript
const readOnlyDevPolicy = `
【読み取り専用開発環境ポリシー】

基本原則：
- コードの学習と理解を促進
- 誤った変更によるトラブルを防止
- 必要な情報へのアクセスは確保

許可される操作：
- すべてのファイルの読み取り（read, list, search）
- ドキュメントの参照
- ログファイルの確認
- テストの実行（読み取り専用）

禁止される操作：
- ファイルの作成・編集・削除
- Gitコミット・プッシュ
- データベースの変更
- 設定ファイルの修正

必須対応：
- アクセスログの記録
- 書き込み試行時の通知
`;
```

**使用例**:
```bash
# 許可される操作
"package.jsonの内容を見せて" → ✅ 許可
"srcディレクトリの構造を表示" → ✅ 許可

# 拒否される操作
"新しいファイルを作成" → ❌ 拒否
"関数を修正して" → ❌ 拒否
```

### 2. 時間制限付きアクセス

**シナリオ**: 営業時間外のシステムアクセスを制限

**ポリシー設定**:
```javascript
const businessHoursPolicy = `
【営業時間アクセス制御ポリシー】

基本原則：
- 営業時間内（平日9-18時）は通常アクセス
- 時間外は緊急対応のみ
- 週末・祝日は特別許可が必要

時間帯別制御：
営業時間内（平日9-18時）:
- すべての通常操作を許可
- 標準的なログ記録

時間外（平日18-21時）:
- 読み取り操作のみ許可
- 書き込みは緊急フラグ付きのみ
- 詳細ログ記録と通知

深夜・早朝（21-9時）:
- オンコール担当者のみアクセス可
- すべての操作に承認必要
- リアルタイム通知

週末・祝日:
- 事前登録された担当者のみ
- 操作理由の記録必須
`;
```

### 3. データ分類に基づく制御

**シナリオ**: データの機密度に応じてアクセスを制御

**ポリシー設定**:
```javascript
const dataClassificationPolicy = `
【データ分類アクセスポリシー】

データ分類：
- 極秘（Top Secret）: 役員のみ
- 機密（Confidential）: 管理職以上
- 社外秘（Internal）: 正社員
- 公開（Public）: 全員

極秘データ：
- アクセスには2要素認証必須
- アクセス履歴を30日間保存
- データのダウンロード禁止
- 画面キャプチャ防止

機密データ：
- 業務目的の明示必須
- アクセス後24時間で自動ロック
- 外部送信時は暗号化必須

社外秘データ：
- 社内ネットワークからのみアクセス可
- 一括ダウンロード制限（100件まで）

制約事項：
- 個人情報を含むデータの外部送信禁止
- 退職者のアクセス権は即座に削除
`;
```

## 🔧 応用的なシナリオ

### 1. CI/CDパイプライン統合

**シナリオ**: 自動化ツールからの安全なアクセス

**設定例**:
```javascript
const cicdPolicy = `
【CI/CDパイプラインポリシー】

基本原則：
- 自動化プロセスの効率性を維持
- セキュリティリスクを最小化
- 監査証跡の完全性を確保

許可される操作：
- ソースコードの読み取り
- テストの実行
- ビルド成果物の作成
- 指定ディレクトリへの書き込み

制限事項：
- 本番環境への直接デプロイ禁止
- 機密情報を含むファイルへのアクセス禁止
- システム設定の変更禁止

特別ルール：
- GitHub Actionsからのアクセス：
  - 特定のワークフローのみ許可
  - シークレットの使用を監視
  
- Jenkins からのアクセス：
  - ジョブごとに権限を設定
  - 並列実行数の制限
`;
```

**統合方法**:
```yaml
# .github/workflows/build.yml
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      AEGIS_AGENT_ID: "github-actions-${{ github.run_id }}"
      AEGIS_PURPOSE: "automated-build"
```

### 2. マルチテナント環境

**シナリオ**: 複数の顧客データを扱うSaaS環境

**設定例**:
```javascript
const multiTenantPolicy = `
【マルチテナント分離ポリシー】

基本原則：
- テナント間の完全なデータ分離
- クロステナントアクセスの防止
- テナント別の監査ログ

アクセス制御：
- エージェントはテナントIDを持つ
- テナントIDと一致するデータのみアクセス可
- 管理者も他テナントデータは参照不可

特別な役割：
スーパー管理者：
- 全テナントの統計情報のみ参照可
- 個別データへのアクセスは緊急時のみ
- アクセス時は全テナントに通知

サポートエンジニア：
- 顧客の明示的な許可でアクセス可
- 時限的アクセス（最大24時間）
- 操作内容の録画必須
`;
```

### 3. コンプライアンス対応

**シナリオ**: GDPR/CCPA等の規制要件への対応

**設定例**:
```javascript
const compliancePolicy = `
【データプライバシーコンプライアンスポリシー】

GDPR要件：
- 個人データへのアクセスは目的を明示
- アクセスログを7年間保存
- データ主体からの要求に72時間以内に対応
- EU域外へのデータ転送は事前承認必須

削除要求への対応：
- 削除要求受信後30日以内に実行
- 削除前にバックアップを作成
- 削除証明書の自動生成
- 関連システムへの削除通知

データ最小化：
- 必要最小限のデータのみ取得
- 90日経過したログは自動匿名化
- 不要なデータの定期削除

監査対応：
- 全アクセスの証跡を暗号化保存
- 監査官への読み取り専用アクセス
- レポートの自動生成機能
`;
```

## 🎯 ベストプラクティス

### 1. 段階的な導入

**フェーズ1: 監視のみ**
```javascript
const monitoringPolicy = `
初期導入ポリシー：
- すべてのアクセスを許可
- 詳細なログ記録のみ実施
- アクセスパターンを分析
`;
```

**フェーズ2: 警告モード**
```javascript
const warningPolicy = `
警告モードポリシー：
- リスクの高い操作に警告表示
- ユーザーに確認を求める
- 実行は許可するがログ強化
`;
```

**フェーズ3: 本格運用**
```javascript
const productionPolicy = `
本番ポリシー：
- リスクに応じた制御を実施
- 必要に応じてブロック
- 例外処理フローの確立
`;
```

### 2. 例外処理の設計

```javascript
const exceptionHandlingPolicy = `
【例外処理ポリシー】

緊急時オーバーライド：
- インシデント発生時は制限を一時解除
- ただし、以下の条件が必要：
  1. インシデントチケットの発行
  2. 上長の承認（電子署名）
  3. 時限設定（最大4時間）
  4. 全操作の録画

例外申請プロセス：
- Slackで申請を受付
- 自動的にチケット作成
- 承認者に通知
- 承認後、一時的な権限付与
`;
```

### 3. パフォーマンスとセキュリティのバランス

```javascript
const performanceOptimizedPolicy = `
【パフォーマンス最適化ポリシー】

キャッシュ戦略：
- 低リスク操作の判定は30分キャッシュ
- ユーザー属性は1時間キャッシュ
- 高リスク操作は常にリアルタイム判定

バッチ処理：
- 同一エージェントの連続リクエストはバッチ化
- 判定結果をまとめて返却
- ログも集約して記録

負荷分散：
- ピーク時は判定を簡略化
- 重要でない制約は後回し
- 事後的な監査で補完
`;
```

## 📊 効果測定

### KPIの設定例

1. **セキュリティ指標**
   - 不正アクセス試行の検出率
   - ポリシー違反の削減率
   - インシデント対応時間

2. **運用指標**
   - 誤検知率（False Positive）
   - ユーザー満足度
   - 平均判定時間

3. **コンプライアンス指標**
   - 監査要求への対応時間
   - ポリシー準拠率
   - ドキュメント完全性

### モニタリングダッシュボード

```javascript
// カスタムダッシュボードの例
const customDashboard = {
  widgets: [
    {
      type: "chart",
      title: "アクセス傾向",
      query: "SELECT hour, COUNT(*) FROM access_logs GROUP BY hour"
    },
    {
      type: "alert",
      title: "異常検知",
      condition: "deny_rate > 0.3"
    },
    {
      type: "list",
      title: "最新の拒否",
      query: "SELECT * FROM denials ORDER BY timestamp DESC LIMIT 10"
    }
  ]
};
```

## 🔗 関連リソース

- [ポリシー記述ガイド](./policy-writing.md) - より詳細なポリシーの書き方
- [トラブルシューティング](../admin-guide/troubleshooting.md) - 問題解決のヒント
- [API リファレンス](../developer-guide/api-reference.md) - プログラマティックな統合